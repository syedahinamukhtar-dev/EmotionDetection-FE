# Public API base URL (no trailing slash). Required for production builds and for
# browser calls from the deployed site. Set the same name in Vercel → Project → Settings → Environment Variables.
NEXT_PUBLIC_API_URL=https://your-backend-host.example.com
